[package]
authors = ["Jorge Aparicio <jorge@japaric.io>"]
edition = "2018"
license = "MIT OR Apache-2.0"
name = "bootloader"
publish = false
version = "0.0.0"

[profile.release]
codegen-units = 1
debug = true
lto = true
opt-level = 'z'

[workspace]